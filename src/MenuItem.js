import Button from '@mui/material/Button'

export default function MenuItem({group, id}) {
    return (
        // <Button>
        <h2 style={{fontFamily: 'Garamond, serif',color: '#000000'}}> {group} </h2>
        /* </Button> */
    )
}